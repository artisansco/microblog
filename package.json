{
  "name": "microblog",
  "description": "",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "install": "npm i -ws=web && npm i -ws=api",
    "web": "npm run dev --workspace=web",
    "api": "npm run dev --workspace=api",
    "format": "npm run check --workspace=web && npm run check --workspace=api"
  },
  "workspaces": [
    "web",
    "api",
    "shared"
  ],
  "devDependencies": {
    "lefthook": "^1.11.14"
  }
}
